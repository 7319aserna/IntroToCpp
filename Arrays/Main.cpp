#include<iostream>
#include "arrayUtils.h"

int main()
{

	// Closed Exercises

	int values[]{ 5, 20, 13, 22, 1};

	//arrayOfNumbers(values, 5);
	//return 0;

	// Find a value
	//int indx = findIndex(values, 5, 3, 0);

	// Reverse
	reverse(values, 5);

	return 0;
}