#include<iostream>
#include "arrayUtils.h"

int main()
{

	// Closed Exercises
	/*
	int values[]{ 5, 20, 13, 22, 1};

	arrayOfNumbers(values, 5);
	*/

	// Sum of Array
	int values[]{ 2, 4, 8, 16, 32, 64 };

	sumOfArray(values, 6);

	return 0;
}